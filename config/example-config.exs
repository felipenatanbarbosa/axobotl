import Config

# Esse arquivo serve apenas para mostrar como o config.exs deve ser
# Para usar o código, o token do bot deve ser colocado em "token" e o arquivo renomeado para "config.exs"

config :nostrum,
token: ""
